/**
 * THE PLATFORM'S INVENTORY FEED — GENERATED FILE, DO NOT EDIT BY HAND.
 *
 * Written by `node scripts/sync-listings.mjs` before every build when
 * `LISTINGS_FEED_URL` is set. `null` means no feed is connected, and the
 * site's lot state comes from `lotState` in `lib/homes.ts`.
 *
 * Commit it as `null`. A local sync rewrites it; `git checkout` it after.
 */
import type { Inventory } from "./inventory";

export const inventory: Inventory | null = null;
